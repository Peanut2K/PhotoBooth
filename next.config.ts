import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // output: "export",
  // images: { unoptimized: true },
  // trailingSlash: true,
  // skipTrailingSlashRedirect: true,
  // assetPrefix: process.env.NODE_ENV === 'production' ? '/PhotoBooth' : '',
  // basePath: process.env.NODE_ENV === 'production' ? '/PhotoBooth' : '',
};

export default nextConfig;
